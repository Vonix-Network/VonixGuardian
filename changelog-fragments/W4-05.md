### W4-05 — Forge/NeoForge mixin infrastructure

- Added `META-INF/vg.mixins.json` mixin config to each Forge/NeoForge cell (1.18.2, 1.19.2, 1.20.1 Forge; 1.21.1 NeoForge) with `minVersion 0.8`, `refmap vg.refmap.json`, cell-specific `package` (`network.vonix.guardian.mc.<v>.forge.mixin` / `.neoforge.mixin`), and an empty `mixins` array — individual mixins land under W4-06.
- Declared `[[mixins]] config = "vg.mixins.json"` in each cell's `mods.toml` / `neoforge.mods.toml`.
- Server-only sides preserved; no source `.java` touched.
